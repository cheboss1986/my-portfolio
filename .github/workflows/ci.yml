name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
 
jobs:
  
  build:
   
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t nchepketer/my-portfolio .
      - name: push image
      - run: 
             docker log -u nchepketer -p ${{secrets.REGISTRY_PASS}}
             docker push  nchepketer/my-portfolio